# 操作：来自目录

目标：在所选连接主机上，从现有目录创建子数据集。

条件：

- 在 `内容 <pool>` 中选择数据集。
- 选择对象必须是数据集（不能是快照）。

行为：

- 打开对话框，定义新数据集并选择源目录。
- 使用安全的临时挂载流程创建数据集。
- 复制内容并保留元数据/权限。
- 当复制/校验成功后：
  - 可选删除源目录（复选框）；
  - 若不删除源目录，数据集保持未挂载，避免覆盖本地目录。
- 命令与进度会写入组合日志。
