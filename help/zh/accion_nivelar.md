# 操作：对齐

目标：使用数据集/快照对齐源与目标状态。

条件：

- 源：数据集或快照。
- 目标：数据集。
- 源和目标都必须使用 OpenZFS `2.3.3` 或更高版本。

行为：

- 根据选择与远端状态计算命令。
- 执行前进行检查。
- 如果源与目标是两个不同的远程 SSH 连接，会尽量直接从源端传到目标端，而不让数据流经过运行 ZFSMgr 的机器。
- INFO 级别记录子命令。
- 该动作会先加入 `待执行更改`，只有在应用待执行更改时才真正执行。
- 若任一端低于 `2.3.3`，该操作会被阻止。
