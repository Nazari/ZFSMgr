# 操作：克隆

> **如何发起.** 在源上点右键（`标记为源`），然后在**目标节点**上打开右键菜单：`以 … 为源` 子菜单中即有该操作。不再有按钮。若显示为灰色，原因见其提示。参见`右键菜单`。

目标：使用 `zfs clone` 将快照克隆到目标数据集。

按钮可用条件：

- 源必须是 `快照`。
- 目标必须是 `数据集`（不能选择快照）。
- 源和目标必须在同一连接中。
- 源和目标必须属于同一个池。

克隆窗口可选参数：

- `-p` 如果父数据集不存在则自动创建。
- `-u` 不自动挂载克隆结果。
- `-o property=value`（每行一条）用于设置克隆属性。

基础命令：

`zfs clone [-p] [-u] [-o property=value]... <source@snapshot> <target_dataset>`

说明：

- 不满足条件时，按钮会保持禁用。
- 操作完成后会刷新目标连接及其数据集视图。
