# 上下文菜单

当前 GUI 主要使用可见按钮进行操作。

## 当前流程

- 在 `连接`、`新建`、`操作` 区域使用按钮。
- 在 `内容 <pool>` 下方使用数据集操作按钮。
- 在 `数据集属性` 中通过 `应用更改` 提交修改。

## 上下文菜单

若你的构建中仍出现局部右键菜单，遵循同样规则：
- 执行操作期间，不安全选项会被禁用；
- 破坏性操作始终需要确认。

## 建议

- 执行前先阅读确认窗口。
- 需要清空源/目标选择时，可使用 `Reset`。
