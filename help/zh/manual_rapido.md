# 快速手册

ZFSMgr 用于管理连接和 ZFS 操作。

- 左侧面板：
- `连接`：连接与池的树形列表。
- `新建`：新建连接或新建池。
- `操作`：传输与高级操作。
- 右侧面板（选中池时）：
- `属性 <pool>`：池属性（上）与池状态（下）。
- `内容 <pool>`：数据集内容（上）与数据集属性（下）。
- 日志：`应用` 与 `SSHs` 标签页。

请查看“快捷键与状态”了解按钮启用条件。
