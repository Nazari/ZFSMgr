# 快捷键与状态

- 执行操作期间按钮会禁用。
- 操作和刷新期间鼠标显示忙碌状态。
- 部分操作需要有效选择（数据集或快照）。
- 当源和目标相同，某些传输会被阻止。
- 在 `数据集属性` 中，如果你编辑了值然后切换标签/焦点，待应用修改会被保留。
- 返回同一数据集时，会恢复已编辑值，且 `应用更改` 在应用或撤销前保持可用。

常见状态：

- OK：连接可用。
- KO/Error：连接或命令失败。
- 已挂载/未挂载：数据集当前状态。
- 不可导入池：会在 `连接状态` 中显示并带原因说明。
